#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @c: the integer to be computed
 * Return: absolute value
 */

int _abs(int c)
{
	if (c >= 0)

		return (t);

	else
		return (-t);
}
